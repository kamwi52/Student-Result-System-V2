<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Class Report Cards</title>
    <style>
        body { font-family: 'Helvetica', 'Arial', sans-serif; margin: 25px; color: #333; }
        .header { text-align: center; margin-bottom: 25px; border-bottom: 2px solid #333; padding-bottom: 15px; }
        h1 { margin: 0; font-size: 24px; }
        h2 { margin: 5px 0; font-size: 18px; font-weight: normal; color: #555; }
        .student-info { margin-bottom: 25px; line-height: 1.6; }
        .student-info table { width: 100%; }
        .student-info td { padding: 2px 0; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; font-size: 14px; }
        th, td { border: 1px solid #ddd; padding: 10px; text-align: left; }
        th { background-color: #f2f2f2; font-weight: bold; }
        .summary td { font-weight: bold; background-color: #f9f9f9; }
        .footer { margin-top: 40px; text-align: center; font-size: 12px; color: #777; border-top: 1px solid #ccc; padding-top: 10px; }
        .page-break { page-break-after: always; }
    </style>
</head>
<body>

    @foreach ($allReportsData as $studentReport)
        @php
            $student = $studentReport['student'];
            $reportData = $studentReport['reportData'];
        @endphp

        <div class="header">
            <h1>Linda Secondary School</h1>
            <h2>Academic Report Card - {{ $academicSession }}</h2>
        </div>

        <div class="student-info">
            <table>
                <tr>
                    <td><strong>Student:</strong></td>
                    <td>{{ $student->name }}</td>
                    <td><strong>Student ID:</strong></td>
                    <td>{{ $student->id }}</td>
                </tr>
                <tr>
                    <td><strong>Class:</strong></td>
                    <td>{{ $classSectionName }}</td>
                    <td><strong>Date Issued:</strong></td>
                    <td>{{ date('F j, Y') }}</td>
                </tr>
            </table>
        </div>

        <h3>Academic Performance Summary</h3>
        <table>
            <thead>
                <tr>
                    <th style="width: 50%;">Subject</th>
                    <th style="width: 25%;">Final Grade / Average</th>
                    <th style="width: 25%;">Remark</th>
                </tr>
            </thead>
            <tbody>
                @forelse ($reportData as $subjectName => $data)
                    <tr class="summary">
                        <td>{{ $subjectName }}</td>
                        <td>{{ number_format($data['final_grade'], 2) }}</td>
                        <td>
                             @if($data['final_grade'] >= 90) Outstanding @elseif($data['final_grade'] >= 80) Excellent @elseif($data['final_grade'] >= 70) Very Good @elseif($data['final_grade'] >= 60) Good @elseif($data['final_grade'] >= 50) Satisfactory @else Needs Improvement @endif
                        </td>
                    </tr>
                @empty
                    <tr>
                        <td colspan="3" style="text-align: center;">No results found for this period.</td>
                    </tr>
                @endforelse
            </tbody>
        </table>

        <div class="footer">
            <p>This is an official document generated by the R-System. | Principal's Signature: _________________________</p>
        </div>

        @if (!$loop->last)
            <div class="page-break"></div>
        @endif

    @endforeach

</body>
</html>