<x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-gray-800 dark:text-gray-200 leading-tight">
            {{ __('Edit Grade') }}
        </h2>
    </x-slot>

    <div class="py-12">
        <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
            <div class="mb-4">
                <a href="{{ route('teacher.gradebook.results', ['assignment' => $assignment->id, 'assessment' => $result->assessment_id]) }}" class="text-sm text-blue-600 hover:underline">← Back to Results</a>
            </div>
            <x-success-message /> {{-- For success messages --}}
            <x-validation-errors class="mb-4" /> {{-- For validation errors --}}

            <div class="bg-white dark:bg-gray-800 overflow-hidden shadow-sm sm:rounded-lg">
                <div class="p-6 text-gray-900 dark:text-gray-100">
                    <h3 class="text-lg font-bold mb-1">Student: <span class="font-normal">{{ $result->student->name ?? 'N/A' }}</span></h3>
                    <h3 class="text-lg font-bold mb-4">Assessment: <span class="font-normal">{{ $assignment->name }}</span></h3> {{-- Using assignment name for context --}}

                    <form action="{{ route('teacher.results.update', $result->id) }}" method="POST">
                        @csrf
                        @method('PUT') {{-- Required for update method --}}

                        <div class="mb-4">
                            <label for="score" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Score</label>
                            <input type="number" name="score" id="score" 
                                   value="{{ old('score', $result->score ?? '') }}" 
                                   min="0" max="{{ $assignment->max_marks }}" step="0.01"
                                   class="mt-1 block w-full rounded-md border-gray-300 dark:border-gray-600 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50 dark:bg-gray-900 dark:text-gray-100">
                            @error('score')
                                <p class="text-red-500 text-xs mt-1">{{ $message }}</p>
                            @enderror
                        </div>

                        {{-- Auto-Generated Remark - Display only --}}
                        <div class="mb-4">
                            <label for="auto_remark_display" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Auto-Generated Remark</label>
                            <input type="text" id="auto_remark_display" 
                                   value="{{ $autoGeneratedRemark ?? 'N/A' }}" 
                                   class="mt-1 block w-full rounded-md border-gray-300 dark:border-gray-600 shadow-sm bg-gray-100 dark:bg-gray-700 dark:text-gray-300" readonly>
                        </div>

                        {{-- Custom Remark - Editable --}}
                        <div class="mb-4">
                            <label for="remark" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Custom Remark (Optional)</label>
                            <input type="text" name="remark" id="remark" 
                                   value="{{ old('remark', $result->remark ?? '') }}" 
                                   maxlength="255"
                                   class="mt-1 block w-full rounded-md border-gray-300 dark:border-gray-600 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50 dark:bg-gray-900 dark:text-gray-100">
                            @error('remark')
                                <p class="text-red-500 text-xs mt-1">{{ $message }}</p>
                            @enderror
                        </div>

                        <div class="flex items-center justify-end mt-6">
                            <a href="{{ route('teacher.gradebook.results', ['assignment' => $assignment->id, 'assessment' => $result->assessment_id]) }}" class="text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100 mr-4">Cancel</a>
                            <button type="submit" class="px-6 py-2 bg-blue-600 text-white font-semibold rounded-md hover:bg-blue-700">
                                Update Grade
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</x-app-layout>